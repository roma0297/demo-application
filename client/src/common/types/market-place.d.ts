export type TStep = {
  id: number,
  title: string,
  finished: boolean,
  active: boolean,
  tooltipText: string,
  description: string
};

export type TObject = {
  [k: string]: any
};

export type TTab = {
  
}